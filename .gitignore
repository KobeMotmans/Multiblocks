# the cache folder for beet, used during the generation but does not conatain any useful info
.beet_cache/
# my python virtual environment, don't include this as it is system specific
.venv/
.vscode/
# Python cache during run, doesn't coantain useful info
__pycache__/
# The export folder is not needed either
export/
# paint-dot-net and task files which we don't need
*.pdn
*.tasks
# these files are specifically for myself
log_terminal_cache.json
__todo__.web
export.json
.venv_pc2_old

# Things used for development
_dev/
beet.json
_build/
done_sound.py
finished.mp3
requirements.txt
