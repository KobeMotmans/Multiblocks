# This function is called through the on_complete callback specified in the json file
# It runs as the marker at their position

# Remove the blueprint when it's complete
function #mtb-generated:basic/house/remove_instance
